using puissance4.board;
using puissance4.player;

namespace test;

public class PlayRealPlayer
{
    [Fact]
    public void PutTokenInRandomColumnSuccessTest()
    {
        /*RealPlayer realPlayer = new RealPlayer('X');
        Board board = new Board();

        int nextMove = realPlayer.GetNextMove();
        bool actualResult = board.PlaceToken(nextMove, realPlayer.Icon);
        
        Assert.True(actualResult);*/
    }

}